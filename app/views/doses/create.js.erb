// Here you generate *JavaScript* that would be executed in the browser
function refreshForm(innerHTML) {
  const newReviewForm = document.getElementById('new_dose');
  newReviewForm.innerHTML = innerHTML;
}

function addDose(reviewHTML) {
  const reviews = document.getElementById('doses');
  doses.insertAdjacentHTML('beforeend', doseHTML);
}

<% if @dose.errors.any? %>
  refreshForm('<%= j render "doses/form", cocktail: @cocktail, dose: @dose %>');
<% else %>
  addDose('<%= j render "doses/show", dose: @dose %>');
  refreshForm('<%= j render "doses/form", cocktail: @cocktail, dose: Dose.new %>');
<% end %>

